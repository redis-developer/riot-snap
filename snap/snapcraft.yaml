# Generated with JReleaser 1.7.0-SNAPSHOT at 2023-06-07T16:57:04.670996308Z
name: riot
version: "3.0.3"
summary: Get data in and out of Redis with RIOT
description: |
  RIOT is a series of command-line utilities designed to help you get data in and out of Redis.
  
  It supports many different sources and targets:
   - Relational databases
   - Files (CSV, JSON, XML)
   - Data generators (Redis data structures, Faker)
   - and Redis itself with live and/or snapshot replication.

grade: stable
confinement: strict
base: core18
type: app

architectures:
  - build-on: [amd64, arm64]
    
    
apps:
  riot:
    command: bin/riot
    environment:
      JAVA_HOME: "$SNAP/usr/lib/jvm/java"
      PATH: "$SNAP/bin:$PATH:$JAVA_HOME/bin"
    plugs:
      - network
      - home

plugs:
  dot-config-files:
    interface: personal-files
parts:
  riot:
    plugin: dump
    source: https://github.com/redis-developer/riot/releases/download/v3.0.3/riot-3.0.3.tar
    source-checksum: sha256/80a65579e3fae28a1b82086e6b2092bf025ece1a54a4564069c17d589ac0e478
    stage-packages:
      - curl
      - openjdk-20-jre
      - ca-certificates
      - ca-certificates-java
    organize:
      'usr/lib/jvm/java-*': 'usr/lib/jvm/java'
    prime:
      - -usr/lib/jvm/java/lib/security/cacerts
